<footer class="flex flex-col">
    <form action="{{ path('app_newsletter_subscribe') }}" method="POST">
        <div class="mb-10"> {# newsletter #}
            <h2 class="text-center mb-1">Recevez notre newsletter</h2>
            <p class="text-center mb-4">Nos nouveautés et surprises en avant-première en vous inscrivant.</p>
            <div class="flex flex-row justify-center">
                <div class="w-1/3">
                    <input class="text-lg px-6 py-3 border-2 w-full" placeholder="Votre e-mail" name="email" type="email" required>
                </div>
                <div class="bg-(--input-bg-color) border-y-2 border-r-2">
                    <button class="font-bold text-shadow-2xs px-5 text-center w-full h-full cursor-pointer" type="submit">S'ABONNER</button>
                </div>
            </div>
        </div>
    </form>
    <div>

    </div>
    <hr>
    <div class="flex justify-center">
        <span>© {{ "now"|date("Y") }} Maison Kalyste. Fait avec passion.</span>
    </div>
</footer>